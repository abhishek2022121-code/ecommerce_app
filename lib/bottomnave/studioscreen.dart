import 'package:flutter/material.dart';

class Studioscreen extends StatelessWidget {
  const Studioscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Studio screen')));
  }
}

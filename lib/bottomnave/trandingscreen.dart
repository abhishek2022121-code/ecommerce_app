import 'package:flutter/material.dart';

class Trandingscreen extends StatelessWidget {
  const Trandingscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('tranding screen')));
  }
}
